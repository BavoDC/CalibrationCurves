% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/timeROC_archived.R
\name{timeROC}
\alias{timeROC}
\title{Compute Time-Dependent ROC Curves and AUC}
\usage{
timeROC(
  T,
  delta,
  marker,
  other_markers = NULL,
  cause,
  weighting = "marginal",
  times,
  ROC = TRUE,
  iid = FALSE
)
}
\arguments{
\item{T}{Vector of observed failure times.}

\item{delta}{Vector of status indicators: \code{0} for censoring, \code{1}
for the event of interest, \code{2} or higher for competing events.}

\item{marker}{Vector of marker values.}

\item{other_markers}{Optional matrix of additional markers associated with
the censoring mechanism. Default is \code{NULL}.}

\item{cause}{The value of \code{delta} that indicates the main event of
interest.}

\item{weighting}{Weighting technique for IPCW. One of \code{"marginal"}
(Kaplan-Meier, default), \code{"cox"} (Cox proportional hazards model),
or \code{"aalen"} (additive Aalen model).}

\item{times}{Vector of time points at which to compute the time-dependent AUC.}

\item{ROC}{Logical; if \code{TRUE} (default), the true positive fraction
(sensitivity) and false positive fraction (1-specificity) are saved for
each time in \code{times}.}

\item{iid}{Logical; if \code{TRUE}, computes the IID representation of the
AUC estimator. Default is \code{FALSE}.}
}
\value{
An object of class \code{"ipcwcompetingrisksROC"} (with competing
  risks) or \code{"ipcwsurvivalROC"} (without competing risks), containing:
  \itemize{
    \item \code{AUC}: vector of AUC values for each time point.
    \item \code{TP}, \code{FP}: (if \code{ROC = TRUE}) matrices of true and
      false positive fractions.
    \item \code{times}: the (ordered) input time points.
    \item \code{n}: number of observations.
    \item \code{Stats}: matrix with case/control/censoring counts per time point.
    \item \code{weights}: IPCW weight object.
    \item \code{computation_time}: elapsed computation time.
  }
}
\description{
Computes time-dependent AUC for survival models using inverse probability of
censoring weighting (IPCW).
}
\details{
This function is a verbatim copy from the \pkg{timeROC} package (v0.4) by
Paul Blanche and Mickaël Foucher, which was archived on CRAN in 2024
(\url{https://cran.r-project.org/package=timeROC}). It is included here
without modification to ensure continued functionality of the
\pkg{CalibrationCurves} package after the dependency was removed.
}
\keyword{internal}
